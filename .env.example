# Database Configuration
DB_TYPE=sqlite              # or "postgres"
DB_PATH=./data/campus_o_network.db  # Only for SQLite

# PostgreSQL (if using postgres)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=campus_o_network
DB_USER=postgres
DB_PASS=password

# API Configuration
PORT=8080
APP_ENV=development
JWT_KEY=your-super-secret-jwt-key